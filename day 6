//Строку в число
const stringToNumber = function(str){
  return +str;  // варианты: parseInt(str); Number(str);
  return null;
}
/////////////////////////////
//Вставить значение  в строку
function greet(name){
  return `Hello, ${name} how are you doing today?`; 
}
/////////////////////////////
