function evenOrOdd(number) {
 if (number % 2 === 0) {
   return "Even";
 } 
  return "Odd";
}

///////////////////////////

function removeChar(str){
 let remove = str.slice(1,-1);
  return remove;
}

